@model List<Hotel.WebApp.Models.Usuario.UsuarioDTO>

<table class="table" id="tableData">
    <thead>
        <tr>
            <td class="text-bold">Id</td>
            <td class="text-bold">Nombre</td>
            <td class="text-bold">Nombre Usuario</td>
            <td class="text-bold">Correo Electrónico</td>
            <td class="text-bold">Acciones</td>
        </tr>
    </thead>
    <tbody>
        @if (Model.Any())
        {
            @foreach (var usuario in Model)
            {
                <tr>
                    <td>@usuario.Id</td>
                    <td>@usuario.TcNombre</td>
                    <td>@usuario.TcNombreUsuario</td>
                    <td>@usuario.TcCorreo</td>
                    <td>
                        @*
                <a asp-controller="Usuario"
                asp-action="Editar"
                asp-route-id="@usuario.Id"
                class="btn btn-sm btn-success text-center">
                <i class="fa fa-pencil" aria-hidden="true"></i>
                </a>
                *@
                        <a asp-controller="Usuario"
                           asp-action="Eliminar"
                           asp-route-id="@usuario.Id"
                           id="btnDelete"
                           class="btn btn-sm btn-danger text-center">
                            <i class="fa fa-trash-o" aria-hidden="true"></i>
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center"><span class="text-danger">No hay registros disponibles.</span></td>
            </tr>
        }
    </tbody>
</table>