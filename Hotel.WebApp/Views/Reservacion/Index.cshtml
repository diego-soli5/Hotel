@model List<Hotel.WebApp.Models.Reservacion.ReservacionDTO>
@{
    ViewData["Title"] = "Reservaciones";
}
<section class="content-header">
    <div class="text-center">
        <h1>GESTIONAR RESERVACIONES</h1>
    </div>
</section>

<section class="content invoice">

    @{
        await Html.RenderPartialAsync("_ReservacionTablePartial", Model);
    }

    <div class="">
        <a asp-controller="Reservacion"
           asp-action="Crear"
           class="btn btn-primary">Crear Nuevo</a>
    </div>
</section>

@section Scripts{
    <script src="~/js/reservacion.js"></script>

    @if (TempData["ActionResult"] != null)
    {
        <script>
            toastr["success"]("@TempData["ActionResult"].ToString()", "@TempData["ActionResultType"].ToString()")
        </script>
    }
}