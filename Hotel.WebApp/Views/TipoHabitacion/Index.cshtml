@model List<Hotel.WebApp.Models.TipoHabitacion.TipoHabitacionDTO>
@{
    ViewData["Title"] = "Tipos de Habitacion";
}
<section class="content-header">
    <div class="text-center">
        <h1>GESTIONAR TIPOS DE HABITACIÓN</h1>
    </div>
</section>

<section class="content invoice">

    @{
        await Html.RenderPartialAsync("_TipoHabitacionTablePartial", Model);
    }

    <div class="">
        <a asp-controller="TipoHabitacion"
           asp-action="Crear"
           class="btn btn-primary">Crear Nuevo</a>
    </div>
</section>

@section Scripts{
    <script src="~/js/tipoHabitacion.js"></script>

    @if (TempData["ActionResult"] != null)
    {
        <script>
            toastr["success"]("@TempData["ActionResult"].ToString()", "@TempData["ActionResultType"].ToString()")
        </script>
    }
}